<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            font-family: 'Times New Roman', Times, serif;
        }

        h1 {
            border: 2px solid black;
            display: inline-block;
            padding: 0 5px 0 5px;
            margin-top: 0px;
            color: brown;
        }

        h1:hover {
            font-size: 40px;
            background-color: black;
            font-family: Arial, Helvetica, sans-serif;
            color: white;
            transition: 0.6s ease-out;
        }

        button {
            margin-top: 10px;
            border: 1px solid black;
            background-color: black;
            color: white;
            border-radius: 5px;
            font-size: 17px;
            padding: 7px;
        }
        button:hover{
            color: lightblue;
            padding: 9px;
            font-size: 20px;
            transition: 0.5s ease-in-out;
        }
        td{
            font-weight: bold;
        }
        div{
            font-weight: bold; 
            margin:8px; 
            display: inline-block;
        }
        div:hover{
            color:red;
            background-color: black;
            transition: 0.6s ease-out;
            padding: 10px;
        }
    </style>
</head>

<body>
    <h1>Login</h1>
    <form action="/user/login" method="POST">
        <table>
            <tr>
                <td>
                    <label for="email">Email</label>

                </td>
                <td>
                    <input id="email" required type="text" required name="email">
                </td>
            </tr>
            <tr>
                <td>
                    <label for="Password">Password</label>
                </td>
                <td>
                    <input id="Password" required type="text" required name="password">

                </td>
            </tr>
        </table>
        <button type="submit">Login</button>
        <div>
            <% if (locals.error) { %>
               <%= error %>
            <% } %>
        </div>
    </form>
</body>

</html>